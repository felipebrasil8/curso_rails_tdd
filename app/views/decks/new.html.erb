<h1 style="text-align: center;"><%= params[:controller].capitalize %></h1>

<div style="text-align: center; margin-bottom: 25px;">
	<%= link_to "Voltar", decks_path %> 
</div>

<div class="form_deck">
<%= form_for @deck do |f| %>
	<%= f.label :name, 'Nome' %>
	<%= f.text_field :name %>
	<br><br>
	<%= f.submit 'Cadastrar' %>
<% end %>
<div>
